
@{
  Layout = "_Layout";
}

@using Factory.Models;

<h1>Engineers</h1>

@if (@Model.Count == 0)
{
  <p>No Engineers! Add a <li>@Html.Actionlink("new engineer!", "Create")</li></p>
} 

<ul>
  @foreach(Engineer engineer in Model)
  {
    <li>@Html.ActionLink($"{Engineer.EName}", "Details", new { id = engineer.EId })</li>
  }
</ul>